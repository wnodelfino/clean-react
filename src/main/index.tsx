import React from "react";
import ReactDOM from "react-dom";
import { Router } from "@/pressentation/components";
import "@/pressentation/styles/global.scss";

ReactDOM.render(<Router />, document.getElementById("main"));
